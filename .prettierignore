node_modules
# Agent scratch space, including git worktrees holding checkouts of this same
# repository. Without this, `pnpm format` reformats files inside another
# checkout — and the rooted patterns below (drizzle/meta) do not match them.
.claude
.next
.next-profiles
coverage
playwright-report
test-results
public/sw.js
public/sw.js.map
pnpm-lock.yaml
drizzle/meta
dist
LICENSE
# The bare git repository ./scripts/weblate.sh keeps for the local Weblate
# to clone and push to. Git's own files, not this project's.
.weblate
# Rendered email bodies, written by `pnpm email:render` and asserted against
# byte for byte. Prettier would reindent the tables and rewrap the inline
# styles, which is exactly the drift these files exist to catch — and three of
# them are identical to the design handoff, which is worth being able to prove.
tests/fixtures/emails
