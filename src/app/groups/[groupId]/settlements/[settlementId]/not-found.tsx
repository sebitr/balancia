import { GoneEntry } from "@/components/entries/gone-entry";

/** The same, for a repayment that has been removed or turned into an expense. */
export default function SettlementNotFound() {
  return <GoneEntry />;
}
